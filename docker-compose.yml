services:
  backend:
    build: ./backend
    env_file:
      - path: .env
        required: true
    volumes:
      - rag-data:/data

  frontend:
    build: ./frontend
    depends_on:
      - backend

volumes:
  rag-data:

